# RMBTran
### src文件夹下三个package内的代码都是可以独立完成人民币大小写转换的
* cn.waldenlu.def 没有使用java自带的字符串匹配，使用正则匹配，中途没发现一次问题，用sbuffer接收当前返回的字符作为下一个函数的输出接着处理，以期达到最终的目的，发现问题实在是太多了，就像大堤泄洪根本堵不住，放弃了。
* codetest换了一种方式，不过没有检测输入，输入不可以是字符，以及17位的数字输入时会发生溢出。
* finalversion 除了整数的情况没有在末尾加一个“整”字，其他条件都能满足。